The `MyList` class from `0-my_list` module
==========================================

Using `MyList`
--------------

Adding and printing a sorted list:

    >>> MyList = __import__('0-my_list').MyList
    >>> my_list = MyList([3, 1, 2])
    >>> my_list.print_sorted()
    [1, 2, 3]

Adding and printing a sorted list with negative numbers:

    >>> my_list = MyList([5, -1, 0, 3])
    >>> my_list.print_sorted()
    [-1, 0, 3, 5]

Adding and printing a sorted list with duplicates:

    >>> my_list = MyList([2, 2, 1, 1])
    >>> my_list.print_sorted()
    [1, 1, 2, 2]

Adding an empty list:

    >>> my_list = MyList([])
    >>> my_list.print_sorted()
    []
